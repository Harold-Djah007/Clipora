# Versioning

- `2.0.9` is the synchronized mobile/resolver reliability release.
- Mobile build number is `49`.
- `0.8.4-universal-foundation` is retained here as the historical branch foundation marker.
