# Current branch

`feature/universal-resolver-foundation` contains the first GitHub-based Clipora upgrade after the user pushed the repository.

It is backend-first. The Flutter phone-testable mobile wire-up comes next.
