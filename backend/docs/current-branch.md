# Current branch

`feature/universal-resolver-foundation` contains the first GitHub-based Clipora upgrade after the user pushed the repository.

It is phone-ready: Flutter talks to `/api/detect` and `/api/resolve/universal`, Threads stays on Smart Capture, and HLS links can fall back to a local `/api/files` download.
