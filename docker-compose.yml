services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      SESSION_ENCRYPTION_KEY: ${SESSION_ENCRYPTION_KEY:-change-me-before-production}
      SESSION_TTL_MINUTES: ${SESSION_TTL_MINUTES:-1440}
    restart: unless-stopped
